[Service]
ExecStart=
ExecStart=/usr/sbin/auditctl -R /usr/share/audit/audit.rules
ExecStart=/bin/sh -c '[ ! -d /boot/swtpm/ ] || /usr/sbin/auditctl -R /usr/share/audit/swtpm.rules'
ExecStart=/usr/sbin/auditctl -e 2
